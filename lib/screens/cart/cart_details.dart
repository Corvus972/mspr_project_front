import 'package:flutter/material.dart';

class CartDetails extends StatelessWidget {
  const CartDetails({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Cart in the place"),
    );
  }
}
